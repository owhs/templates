Atelier's look lives in `assets/tokens.css` (palette + type scale) and the
overrides at the end of `assets/components.css`. Layout/partials are shared with
Aurora so every native block keeps working — restyle, don't rebuild.
