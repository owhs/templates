<div class="sb-sectors">
  {% for s in props.items %}
    {% if s.href %}
      <a class="sec-cell" href="{{ s.href }}">
        <span class="sec-ico">{{ s.icon | default: 'building' | icon: 24 }}</span>
        <span class="sec-name"{{ 'items' | wbe_at: forloop.index0, 'name' }}>{{ s.name | escape }}</span>
        {% if s.text %}<span class="sec-text"{{ 'items' | wbe_at: forloop.index0, 'text' }}>{{ s.text | escape }}</span>{% endif %}
      </a>
    {% else %}
      <div class="sec-cell">
        <span class="sec-ico">{{ s.icon | default: 'building' | icon: 24 }}</span>
        <span class="sec-name"{{ 'items' | wbe_at: forloop.index0, 'name' }}>{{ s.name | escape }}</span>
        {% if s.text %}<span class="sec-text"{{ 'items' | wbe_at: forloop.index0, 'text' }}>{{ s.text | escape }}</span>{% endif %}
      </div>
    {% endif %}
  {% endfor %}
</div>
