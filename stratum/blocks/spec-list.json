{
  "type": "spec-list",
  "label": "Specification table",
  "group": "Strongman",
  "icon": "table",
  "description": "Clean two-column technical data. Put a metric and imperial pair or a qualifier in one value as \"3200 kg (7050 lb)\" — the bracketed part drops to a muted second line so the figures stay aligned.",
  "fields": [
    { "key": "title", "type": "text", "label": "Title" },
    { "key": "icon", "type": "text", "label": "Icon", "width": "half", "help": "A name from the site icon set, e.g. compare, chart, bolt, gear, shield. Leave blank for no icon." },
    {
      "key": "rows",
      "type": "list",
      "label": "Rows",
      "item": [
        { "key": "label", "type": "text", "label": "Label" },
        { "key": "value", "type": "text", "label": "Value" },
        { "key": "note", "type": "text", "label": "Note (optional)" }
      ]
    },
    { "key": "note", "type": "text", "label": "Footnote", "help": "e.g. 'Indicative figures — confirmed at survey.'" }
  ],
  "defaults": { "rows": [] }
}
