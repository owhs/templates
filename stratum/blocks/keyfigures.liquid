<div class="sb-rail" style="--n:{{ props.items | size }}">
  {% for f in props.items %}
    <div class="rail-cell">
      <span class="rail-k"{{ 'items' | wbe_at: forloop.index0, 'k' }}>{{ f.k | escape }}</span>
      <span class="rail-v"{{ 'items' | wbe_at: forloop.index0, 'v' }}>{{ f.v | escape }}</span>
      {% if f.n %}<span class="rail-n"{{ 'items' | wbe_at: forloop.index0, 'n' }}>{{ f.n | escape }}</span>{% endif %}
    </div>
  {% endfor %}
</div>
