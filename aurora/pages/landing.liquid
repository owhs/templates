{%- comment -%}
  Landing — a conversion page: no site footer noise, tighter header spacing.
  The header stays (brand + nav) but the page opens straight into content.
{%- endcomment -%}
<div class="tpl-landing">
  {% region 'content' %}
</div>
<style>
  .tpl-landing .wb-section:first-child { padding-top: clamp(2rem, 6vw, 4.5rem); }
  .tpl-landing { --container: 64rem; }
</style>
